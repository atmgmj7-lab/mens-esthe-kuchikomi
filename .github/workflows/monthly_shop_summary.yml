name: Monthly Shop Summary Staging Notice

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staging-not-ready:
    runs-on: ubuntu-latest
    steps:
      - name: Explain why public writes are disabled
        run: |
          echo "Monthly official-site collection is disabled."
          echo "No WordPress write will run until the Supabase staging and approval plan is complete."
